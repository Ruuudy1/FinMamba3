# Non-CUDA dependency list for the Docker / Colab flows. It mirrors [project.dependencies] in
# pyproject.toml, which is the single source of truth for the installed package; keep the two in
# sync. The CUDA-built packages (torch, mamba_ssm, causal_conv1d) are installed separately in the
# Dockerfile / Colab, not listed here.
setuptools==69.5.1
packaging>=24.0
ninja>=1.11
PyYAML>=6.0
einops==0.8.0
tqdm==4.66.4
numpy>=2,<3
pandas>=2.2,<3
pyarrow>=15.0
gymnasium>=1.0,<2
wandb
pytorch_warmup==0.1.1
